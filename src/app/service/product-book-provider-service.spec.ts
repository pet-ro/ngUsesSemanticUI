import { ProductBookProviderService } from './product-book-provider-service';

describe('ProductBookProviderService', () => {
  it('should create an instance', () => {
    expect(new ProductBookProviderService()).toBeTruthy();
  });
});
